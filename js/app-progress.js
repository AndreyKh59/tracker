// ============================================
// FitPulse — app-progress.js
// Вкладка «Прогресс»
// ============================================

(function() {
  'use strict';

  var FP = window.FitPulse;

  // Пока заглушка — прогресс будет добавлен позже
  // Функция рендера зарезервирована на случай расширения
  FP.renderProgressView = function(profile) {
    // Placeholder — раздел прогресса будет добавлен на следующем этапе
  };

})();
